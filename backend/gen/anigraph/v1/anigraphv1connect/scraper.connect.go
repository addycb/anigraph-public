// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: anigraph/v1/scraper.proto

package anigraphv1connect

import (
	v1 "anigraph/backend/gen/anigraph/v1"
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ScraperServiceName is the fully-qualified name of the ScraperService service.
	ScraperServiceName = "anigraph.v1.ScraperService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ScraperServiceScrapeIncrementalProcedure is the fully-qualified name of the ScraperService's
	// ScrapeIncremental RPC.
	ScraperServiceScrapeIncrementalProcedure = "/anigraph.v1.ScraperService/ScrapeIncremental"
)

// ScraperServiceClient is a client for the anigraph.v1.ScraperService service.
type ScraperServiceClient interface {
	// ScrapeIncremental fetches new/updated media from AniList starting after max_id.
	// Streams progress updates and then batched results (media, staff, edges, relations).
	ScrapeIncremental(context.Context, *connect.Request[v1.ScrapeIncrementalRequest]) (*connect.ServerStreamForClient[v1.ScrapeIncrementalResponse], error)
}

// NewScraperServiceClient constructs a client for the anigraph.v1.ScraperService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewScraperServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ScraperServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	scraperServiceMethods := v1.File_anigraph_v1_scraper_proto.Services().ByName("ScraperService").Methods()
	return &scraperServiceClient{
		scrapeIncremental: connect.NewClient[v1.ScrapeIncrementalRequest, v1.ScrapeIncrementalResponse](
			httpClient,
			baseURL+ScraperServiceScrapeIncrementalProcedure,
			connect.WithSchema(scraperServiceMethods.ByName("ScrapeIncremental")),
			connect.WithClientOptions(opts...),
		),
	}
}

// scraperServiceClient implements ScraperServiceClient.
type scraperServiceClient struct {
	scrapeIncremental *connect.Client[v1.ScrapeIncrementalRequest, v1.ScrapeIncrementalResponse]
}

// ScrapeIncremental calls anigraph.v1.ScraperService.ScrapeIncremental.
func (c *scraperServiceClient) ScrapeIncremental(ctx context.Context, req *connect.Request[v1.ScrapeIncrementalRequest]) (*connect.ServerStreamForClient[v1.ScrapeIncrementalResponse], error) {
	return c.scrapeIncremental.CallServerStream(ctx, req)
}

// ScraperServiceHandler is an implementation of the anigraph.v1.ScraperService service.
type ScraperServiceHandler interface {
	// ScrapeIncremental fetches new/updated media from AniList starting after max_id.
	// Streams progress updates and then batched results (media, staff, edges, relations).
	ScrapeIncremental(context.Context, *connect.Request[v1.ScrapeIncrementalRequest], *connect.ServerStream[v1.ScrapeIncrementalResponse]) error
}

// NewScraperServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewScraperServiceHandler(svc ScraperServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	scraperServiceMethods := v1.File_anigraph_v1_scraper_proto.Services().ByName("ScraperService").Methods()
	scraperServiceScrapeIncrementalHandler := connect.NewServerStreamHandler(
		ScraperServiceScrapeIncrementalProcedure,
		svc.ScrapeIncremental,
		connect.WithSchema(scraperServiceMethods.ByName("ScrapeIncremental")),
		connect.WithHandlerOptions(opts...),
	)
	return "/anigraph.v1.ScraperService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ScraperServiceScrapeIncrementalProcedure:
			scraperServiceScrapeIncrementalHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedScraperServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedScraperServiceHandler struct{}

func (UnimplementedScraperServiceHandler) ScrapeIncremental(context.Context, *connect.Request[v1.ScrapeIncrementalRequest], *connect.ServerStream[v1.ScrapeIncrementalResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("anigraph.v1.ScraperService.ScrapeIncremental is not implemented"))
}
